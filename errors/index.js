const CustomAPIError = require('./custom-error')
const badRequest = require('./bad-request')
const unAthentication = require('./unAuthentication')

module.exports = {
    CustomAPIError,
    badRequest,
    unAthentication,
}